# ─────────────────────────────────────────────────────
# Sentinel Rule Forge — SUGGESTED RULES
# ─────────────────────────────────────────────────────
# These rules were generated from your request:
#   "Find risky eval usage"
#
# STATUS: For review only. Not active.
# To use: copy rules you approve into sentinel.yml
# Do NOT run --apply until you have reviewed this file.
# ─────────────────────────────────────────────────────

# Generated: 2026-05-05 01:00:38
# Request: "Find risky eval usage"

rules:
- id: risky-eval
  type: usage
  pattern: \beval\s*\(
  message: eval() executes arbitrary code and can be a serious security risk. Review
    carefully.
  severity: error
  suggested_replacement: Consider ast.literal_eval() for safe evaluation of literals.
- id: risky-exec
  type: usage
  pattern: \bexec\s*\(
  message: exec() executes arbitrary code dynamically. Ensure input is fully trusted.
  severity: error
  suggested_replacement: Refactor to avoid dynamic code execution if possible.
- id: risky-compile
  type: usage
  pattern: \bcompile\s*\(
  message: compile() with untrusted input is a security risk similar to eval().
  severity: warning
  suggested_replacement: Review whether compile() is necessary here.
